// importacoes dinamicas para libs mais pesada moment ou outra,
// importa so quando o usuario realmente vai utilizar na aplicacao
export default {
  sum:(a: number,b: number) =>{
    return a+b;
  }
}